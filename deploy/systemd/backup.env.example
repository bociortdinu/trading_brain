# Install to /etc/trading_brain/backup.env with chmod 0600 (it holds the DB password).
# Use an ADMIN/owner DSN so pg_dump can read every object.
BRAIN_BACKUP_DSN=postgresql://postgres:CHANGE_ME@127.0.0.1:5433/trading_brain
BACKUP_DIR=/var/backups/trading_brain
BACKUP_KEEP=30
