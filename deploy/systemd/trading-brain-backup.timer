[Unit]
Description=Daily trading_brain PostgreSQL backup

[Timer]
OnCalendar=*-*-* 02:30:00
Persistent=true          # run on next boot if the machine was off at the scheduled time
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
